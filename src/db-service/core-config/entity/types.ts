export enum ConfigType {
  Core = 'core',
  Feature = 'feature',
}

export enum ConfigSubType {
  Command = 'command',
  Message = 'message',
  Audio = 'audio',
  Signal = 'signal',
  Whatsapp = 'whatsapp',
  Email = 'email',
  Groq = 'groq',
}

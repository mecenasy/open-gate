export const PUBLIC_PATHS = [
  '/',
  '/login',
  '/registration',
  '/reset-password',
  '/forgot-password',
  '/qr-verify',
  '/thankyou',
] as const;

export const OWNER_ONLY_PATHS = ['/core-config'] as const;

export const LOGIN_PATH = '/login';
export const HOME_PATH = '/';
